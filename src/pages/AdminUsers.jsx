import React from 'react';
import { base44 } from '@/api/base44Client';
import { useQuery,useQueryClient } from '@tanstack/react-query';
import AdminAccessGate from '@/components/admin/AdminAccessGate';
import InviteTeamMember from '@/components/admin/InviteTeamMember';
import UserManagementTable from '@/components/admin/UserManagementTable';
import ArtistApplicationsPanel from '@/components/admin/ArtistApplicationsPanel';
function Panel({me}){const cache=useQueryClient();const refresh=()=>cache.invalidateQueries({queryKey:['admin-access']});const {data={users:[],plans:[],applications:[]},isLoading}=useQuery({queryKey:['admin-access'],queryFn:async()=>{const [users,plans,applications]=await Promise.all([base44.entities.User.list('-created_date',100),base44.entities.TeamInvite.list('created_date',20),base44.entities.ArtistApplication.filter({status:'pending'},'-created_date',100)]);return{users,plans,applications};}});if(isLoading)return <p>Carregando equipe...</p>;const isMaster=me.app_role==='master';return <div className="space-y-10"><section><h2 className="mb-4 text-2xl font-bold">Artistas pendentes</h2><ArtistApplicationsPanel items={data.applications} reviewer={me.email} onDone={refresh}/></section>{isMaster&&<><section><h2 className="mb-2 text-2xl font-bold">Convidar equipe</h2><p className="mb-4 text-sm text-muted-foreground">Senhas são privadas; cada integrante define ou recupera a própria senha.</p><InviteTeamMember plans={data.plans} onDone={refresh}/></section><section><h2 className="mb-4 text-2xl font-bold">Usuários e permissões</h2><UserManagementTable users={data.users} onDone={refresh}/></section></>}</div>;}
export default function AdminUsers(){return <AdminAccessGate>{me=><div className="min-h-screen bg-muted/30 py-12"><main className="mx-auto max-w-6xl px-4"><header className="mb-10"><p className="text-sm font-bold uppercase tracking-widest text-emerald-700">Administração</p><h1 className="mt-2 text-4xl font-bold">Equipe e artistas</h1></header><Panel me={me}/></main></div>}</AdminAccessGate>;}